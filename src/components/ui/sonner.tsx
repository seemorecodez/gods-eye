import { CSSProperties } from "react"


function Toaster(props: ToasterProps) {
  return (
    <Sonner
        } as CSSProperties
      {...pro
  )



        } as CSSProperties
      }
      {...props}
    />
  )
}

export { Toaster }
